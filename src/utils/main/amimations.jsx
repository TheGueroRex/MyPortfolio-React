import { gsap } from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";
import { useEffect } from "react";

gsap.registerPlugin(ScrollTrigger);



function amimations() {
  useEffect(() => {

  }, []);

  return null;
}

export default amimations;
